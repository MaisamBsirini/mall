import 'package:flutter/material.dart';

import 'section_showcase.dart';

class BookingShowcase extends StatelessWidget {
  const BookingShowcase({super.key});

  @override
  Widget build(BuildContext context) {
    return const SectionShowcaseById(sectionId: 'booking');
  }
}
