import 'package:flutter/material.dart';

import 'section_showcase.dart';

class CertificationsShowcase extends StatelessWidget {
  const CertificationsShowcase({super.key});

  @override
  Widget build(BuildContext context) {
    return const SectionShowcaseById(sectionId: 'certifications');
  }
}
