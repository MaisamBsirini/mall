import 'package:flutter/material.dart';

import 'section_showcase.dart';

class SocialMediaShowcase extends StatelessWidget {
  const SocialMediaShowcase({super.key});

  @override
  Widget build(BuildContext context) {
    return const SectionShowcaseById(sectionId: 'social');
  }
}
