import 'package:flutter/material.dart';

class SoftShadow {
  static List<BoxShadow> luxury = [
    BoxShadow(
      color: Colors.black.withOpacity(
        0.10,
      ),
      blurRadius: 30,
      offset: const Offset(0, 18),
    ),
  ];
}